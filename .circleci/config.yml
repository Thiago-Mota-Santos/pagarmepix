version: 2.1

env:  
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
jobs:
  preview_deployment:
    docker:
      - image: circleci/node:latest
    environment:
      VERCEL_ORG_ID: $VERCEL_ORG_ID
      VERCEL_PROJECT_ID: $VERCEL_PROJECT_ID
    steps:
      - checkout
      - run:
          name: Install Vercel CLI
          command: sudo npm install --global vercel@latest
      - run:
          name: Pull Vercel Environment Information
          command: vercel pull --yes --environment=preview --token=zXw6aw1fNZlXUbd7JV1vgHDg
      - run:
          name: Build Project Artifacts
          command: vercel build --token=zXw6aw1fNZlXUbd7JV1vgHDg
      - run:
          name: Deploy Project Artifacts to Vercel
          command: vercel deploy --prebuilt --token=zXw6aw1fNZlXUbd7JV1vgHDg

  production_deployment:
    resource_class: medium+
    docker:
      - image: circleci/node:latest
    environment:
      VERCEL_ORG_ID: $VERCEL_ORG_ID
      VERCEL_PROJECT_ID: $VERCEL_PROJECT_ID
    steps:
      - checkout
      - run:
          name: Install Vercel CLI
          command: sudo npm install --global vercel@latest
      - run:
          name: Pull Vercel Environment Information
          command: vercel pull --yes --environment=production --token=zXw6aw1fNZlXUbd7JV1vgHDg
      - run:
          name: Build Project Artifacts
          command: vercel build --prod --token=zXw6aw1fNZlXUbd7JV1vgHDg
      - run:
          name: Deploy Project Artifacts to Vercel
          command: vercel deploy --prebuilt --prod --token=zXw6aw1fNZlXUbd7JV1vgHDg

workflows:
  version: 2
  preview_and_production:
    jobs:
      - preview_deployment:
          filters:
            branches:
              ignore: /main/
      - production_deployment:
          filters:
            branches:
              only: /main/